<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Purchase Admin System</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.1/font/bootstrap-icons.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>

    <button class="mobile-menu-toggle" id="mobileMenuToggle">
        <i class="bi bi-list"></i>
    </button>

    <aside class="sidebar" id="sidebar">
        <div class="sidebar-brand">
            <h2>
                <i class="bi bi-cart-check-fill"></i>
                Admin System
            </h2>
        </div>
        
        <ul class="sidebar-menu">
            <li>
                <a href="/" class="<%= typeof currentPage !== 'undefined' && currentPage === 'dashboard' ? 'active' : '' %>">
                    <i class="bi bi-speedometer2"></i>
                    <span>Dashboard</span>
                </a>
            </li>
            <li>
                <a href="/admin/products" class="<%= typeof currentPage !== 'undefined' && currentPage === 'products' ? 'active' : '' %>">
                    <i class="bi bi-box-seam"></i>
                    <span>Produk & Stok</span>
                </a>
            </li>
            <li>
                <a href="/admin/add-purchase" class="<%= typeof currentPage !== 'undefined' && currentPage === 'add-purchase' ? 'active' : '' %>">
                    <i class="bi bi-plus-circle"></i>
                    <span>Tambah Pembelian</span>
                </a>
            </li>
            <li>
                <a href="/admin/purchases" class="<%= typeof currentPage !== 'undefined' && currentPage === 'purchases' ? 'active' : '' %>">
                    <i class="bi bi-clock-history"></i>
                    <span>Riwayat Pembelian</span>
                </a>
            </li>
        </ul>
    </aside>

    <main class="main-content">
        <div class="content-wrapper">